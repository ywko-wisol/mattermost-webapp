The loading wrapper show the children while is not loading.

Loading example:

```js
<LoadingWrapper loading={true} text="loading">not loading</LoadingWrapper>
```

Not loading example:

```js
<LoadingWrapper loading={false} text="loading">not loading</LoadingWrapper>
```
